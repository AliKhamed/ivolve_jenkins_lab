[defaults]
inventory = ./inventory
remote_user = ubuntu
private_key_file = /home/ali/Downloads/slave.pem 
host_key_checking = False

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
